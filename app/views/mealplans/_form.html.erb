<%= form_for(@mealplan) do |f| %>
  <% if @mealplan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mealplan.errors.count, "error") %> prohibited this mealplan from being saved:</h2>

      <ul>
      <% @mealplan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br />
    <%= f.date_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br />
    <%= f.date_select :end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
